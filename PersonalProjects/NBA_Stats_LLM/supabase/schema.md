## Tables (create via Supabase dashboard)

- players_cache: id, full_name, provider_ids
- teams_cache: id, name, abbr, provider_ids
- queries: id, text, parsed_json, created_at
- results_cache: id, hash_key, json, ttl
